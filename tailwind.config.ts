import type { Config } from "tailwindcss";

const config: Config = {
  // Tailwind v4 uses automatic content detection by default
  // No content array needed unless you want to override detection
  plugins: [],
};

export default config;